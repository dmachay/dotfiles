# Green text, time, and date; windows in blue:
# (From John Anderson's .screenrc)
hardstatus alwayslastline "%{=b}%{G} Screen(s): %{b}%w %=%{kG}%C%A  %D, %M/%d/%Y "

# Turn off start message:
startup_message off

# Set messages timeout to one second:
msgwait 1
